type: object
properties:
  id:
    type: integer
    example: 1
  user_id:
    type: integer
    example: 1
  project_id:
    type: integer
    example: 1
  role:
    type: string
    enum: ['owner', 'admin', 'developer', 'viewer']
    example: 'owner'
  created_at:
    type: string
    format: date-time
  updated_at:
    type: string
    format: date-time
